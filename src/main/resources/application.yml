spring:
  application:
    name: pos

  datasource:
    url: jdbc:postgresql://localhost:5432/pos
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
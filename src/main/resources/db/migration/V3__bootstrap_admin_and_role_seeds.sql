CREATE TABLE app_roles (
    id UUID PRIMARY KEY,
    name VARCHAR(50) NOT NULL UNIQUE
);

INSERT INTO app_roles (id, name)
SELECT '11111111-1111-1111-1111-111111111111', 'SUPER_ADMIN'
WHERE NOT EXISTS (
    SELECT 1
    FROM app_roles
    WHERE name = 'SUPER_ADMIN'
);

INSERT INTO app_roles (id, name)
SELECT '22222222-2222-2222-2222-222222222222', 'ADMIN'
WHERE NOT EXISTS (
    SELECT 1
    FROM app_roles
    WHERE name = 'ADMIN'
);

INSERT INTO app_roles (id, name)
SELECT '33333333-3333-3333-3333-333333333333', 'MANAGER'
WHERE NOT EXISTS (
    SELECT 1
    FROM app_roles
    WHERE name = 'MANAGER'
);

INSERT INTO app_roles (id, name)
SELECT '44444444-4444-4444-4444-444444444444', 'CASHIER'
WHERE NOT EXISTS (
    SELECT 1
    FROM app_roles
    WHERE name = 'CASHIER'
);

ALTER TABLE users
    ADD COLUMN must_change_password BOOLEAN NOT NULL DEFAULT FALSE;
